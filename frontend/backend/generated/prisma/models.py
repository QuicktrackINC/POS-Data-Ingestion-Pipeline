# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class User(bases.BaseUser):
    """Represents a User record"""

    id: _str
    username: _str
    passwordHash: _str
    role: _str
    storeId: Optional[_str] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserKeys']] = None,
        exclude: Optional[Iterable['types.UserKeys']] = None,
        required: Optional[Iterable['types.UserKeys']] = None,
        optional: Optional[Iterable['types.UserKeys']] = None,
        relations: Optional[Mapping['types.UserRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _User_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _User_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "User" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid User / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'User',
            }
        )
        _created_partial_types.add(name)


class ImportRecord(bases.BaseImportRecord):
    """Master record for every XML file upload.
    """

    id: _str
    originalFileName: _str
    """Original file name as uploaded.
    """

    storedFilePath: Optional[_str] = None
    """Path where the raw XML is stored on disk.
    """

    checksum: _str
    """SHA-256 checksum of the raw file bytes.
    """

    fileSize: _int
    """File size in bytes.
    """

    uploadedAt: datetime.datetime
    """When the file was uploaded.
    """

    uploadedBy: _str
    """Who uploaded it (user/system identifier).
    """

    sourceType: Optional[_str] = None
    """Source type: POSExport, NAXML-ItemMaintenanceRequest, etc.
    """

    storeCode: Optional[_str] = None
    """Normalized store code extracted from the file.
    """

    businessDate: Optional[_str] = None
    """Business date the data covers (YYYY-MM-DD).
    """

    parserVersion: _str
    """Parser version used.
    """

    status: 'enums.ImportStatus'
    """Current processing status.
    """

    recordsExtracted: _int
    """Number of records successfully extracted.
    """

    errorCount: _int
    """Number of validation/processing errors.
    """

    rawXml: Optional[_str] = None
    """The full raw XML text preserved for reprocessing.
    """

    events: Optional[List['models.ImportEvent']] = None
    """Timeline of status transitions.
    """

    errors: Optional[List['models.ImportError']] = None
    """All errors/warnings recorded during processing.
    """

    dailySalesSummaries: Optional[List['models.DailySalesSummary']] = None
    """Extracted normalized data
    """

    salesLineItems: Optional[List['models.SalesLineItem']] = None
    departmentSummaries: Optional[List['models.DepartmentSummary']] = None
    fuelRecords: Optional[List['models.FuelRecord']] = None
    productExtracts: Optional[List['models.ProductExtract']] = None
    updatedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ImportRecordKeys']] = None,
        exclude: Optional[Iterable['types.ImportRecordKeys']] = None,
        required: Optional[Iterable['types.ImportRecordKeys']] = None,
        optional: Optional[Iterable['types.ImportRecordKeys']] = None,
        relations: Optional[Mapping['types.ImportRecordRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ImportRecordKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ImportRecord_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ImportRecord_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ImportRecord_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ImportRecord_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ImportRecord_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ImportRecord_relational_fields:
                        raise errors.UnknownRelationalFieldError('ImportRecord', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ImportRecord / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ImportRecord',
            }
        )
        _created_partial_types.add(name)


class ImportEvent(bases.BaseImportEvent):
    """A single event in an import's lifecycle (status change, reprocess, etc.).
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    status: 'enums.ImportStatus'
    """Status at time of event.
    """

    note: Optional[_str] = None
    """Optional human-readable note.
    """

    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ImportEventKeys']] = None,
        exclude: Optional[Iterable['types.ImportEventKeys']] = None,
        required: Optional[Iterable['types.ImportEventKeys']] = None,
        optional: Optional[Iterable['types.ImportEventKeys']] = None,
        relations: Optional[Mapping['types.ImportEventRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ImportEventKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ImportEvent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ImportEvent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ImportEvent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ImportEvent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ImportEvent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ImportEvent_relational_fields:
                        raise errors.UnknownRelationalFieldError('ImportEvent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ImportEvent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ImportEvent',
            }
        )
        _created_partial_types.add(name)


class ImportError(bases.BaseImportError):
    """A validation or processing error linked to an import.
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    errorType: _str
    """Error category: VALIDATION, PROCESSING, DUPLICATE, etc.
    """

    message: _str
    """Human-readable error message.
    """

    field: Optional[_str] = None
    """Optional field/path that caused the error.
    """

    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ImportErrorKeys']] = None,
        exclude: Optional[Iterable['types.ImportErrorKeys']] = None,
        required: Optional[Iterable['types.ImportErrorKeys']] = None,
        optional: Optional[Iterable['types.ImportErrorKeys']] = None,
        relations: Optional[Mapping['types.ImportErrorRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ImportErrorKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ImportError_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ImportError_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ImportError_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ImportError_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ImportError_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ImportError_relational_fields:
                        raise errors.UnknownRelationalFieldError('ImportError', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ImportError / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ImportError',
            }
        )
        _created_partial_types.add(name)


class DailySalesSummary(bases.BaseDailySalesSummary):
    """Normalized daily sales totals per store per date.
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    storeCode: _str
    businessDate: _str
    grossSales: _float
    netSales: _float
    taxAmount: _float
    transactionCount: _int
    averageTicket: _float
    cashAmount: _float
    creditAmount: _float
    debitAmount: _float
    ebtAmount: _float
    fuelSales: _float
    discountAmount: _float
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DailySalesSummaryKeys']] = None,
        exclude: Optional[Iterable['types.DailySalesSummaryKeys']] = None,
        required: Optional[Iterable['types.DailySalesSummaryKeys']] = None,
        optional: Optional[Iterable['types.DailySalesSummaryKeys']] = None,
        relations: Optional[Mapping['types.DailySalesSummaryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DailySalesSummaryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DailySalesSummary_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DailySalesSummary_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DailySalesSummary_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DailySalesSummary_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DailySalesSummary_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DailySalesSummary_relational_fields:
                        raise errors.UnknownRelationalFieldError('DailySalesSummary', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DailySalesSummary / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DailySalesSummary',
            }
        )
        _created_partial_types.add(name)


class SalesLineItem(bases.BaseSalesLineItem):
    """Individual line item extracted from a transaction.
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    storeCode: _str
    businessDate: _str
    transactionId: Optional[_str] = None
    itemName: _str
    itemCode: Optional[_str] = None
    upc: Optional[_str] = None
    department: Optional[_str] = None
    quantity: _float
    unitPrice: _float
    salesAmount: _float
    taxAmount: _float
    productMasterId: Optional[_str] = None
    productMaster: Optional['models.ProductMaster'] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SalesLineItemKeys']] = None,
        exclude: Optional[Iterable['types.SalesLineItemKeys']] = None,
        required: Optional[Iterable['types.SalesLineItemKeys']] = None,
        optional: Optional[Iterable['types.SalesLineItemKeys']] = None,
        relations: Optional[Mapping['types.SalesLineItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SalesLineItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SalesLineItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SalesLineItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SalesLineItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SalesLineItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SalesLineItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SalesLineItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('SalesLineItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SalesLineItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SalesLineItem',
            }
        )
        _created_partial_types.add(name)


class DepartmentSummary(bases.BaseDepartmentSummary):
    """Department-level sales rollup.
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    storeCode: _str
    businessDate: _str
    department: _str
    quantitySold: _float
    grossAmount: _float
    netAmount: _float
    taxAmount: _float
    discountAmount: _float
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DepartmentSummaryKeys']] = None,
        exclude: Optional[Iterable['types.DepartmentSummaryKeys']] = None,
        required: Optional[Iterable['types.DepartmentSummaryKeys']] = None,
        optional: Optional[Iterable['types.DepartmentSummaryKeys']] = None,
        relations: Optional[Mapping['types.DepartmentSummaryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DepartmentSummaryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DepartmentSummary_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DepartmentSummary_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DepartmentSummary_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DepartmentSummary_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DepartmentSummary_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DepartmentSummary_relational_fields:
                        raise errors.UnknownRelationalFieldError('DepartmentSummary', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DepartmentSummary / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DepartmentSummary',
            }
        )
        _created_partial_types.add(name)


class FuelRecord(bases.BaseFuelRecord):
    """Fuel sales record extracted from the import.
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    storeCode: _str
    businessDate: _str
    fuelGrade: _str
    gallons: _float
    salesAmount: _float
    pricePerGallon: _float
    pumpNumber: Optional[_str] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FuelRecordKeys']] = None,
        exclude: Optional[Iterable['types.FuelRecordKeys']] = None,
        required: Optional[Iterable['types.FuelRecordKeys']] = None,
        optional: Optional[Iterable['types.FuelRecordKeys']] = None,
        relations: Optional[Mapping['types.FuelRecordRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FuelRecordKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FuelRecord_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FuelRecord_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FuelRecord_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FuelRecord_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FuelRecord_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FuelRecord_relational_fields:
                        raise errors.UnknownRelationalFieldError('FuelRecord', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FuelRecord / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FuelRecord',
            }
        )
        _created_partial_types.add(name)


class ProductExtract(bases.BaseProductExtract):
    """Raw product data extracted from a file (pre-mapping).
    """

    id: _str
    importId: _str
    importRecord: Optional['models.ImportRecord'] = None
    storeCode: _str
    businessDate: Optional[_str] = None
    rawName: _str
    itemCode: Optional[_str] = None
    upc: Optional[_str] = None
    department: Optional[_str] = None
    unitPrice: Optional[_float] = None
    productMasterId: Optional[_str] = None
    productMaster: Optional['models.ProductMaster'] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductExtractKeys']] = None,
        exclude: Optional[Iterable['types.ProductExtractKeys']] = None,
        required: Optional[Iterable['types.ProductExtractKeys']] = None,
        optional: Optional[Iterable['types.ProductExtractKeys']] = None,
        relations: Optional[Mapping['types.ProductExtractRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductExtractKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductExtract_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductExtract_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductExtract_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductExtract_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductExtract_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductExtract_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductExtract', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductExtract / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductExtract',
            }
        )
        _created_partial_types.add(name)


class ProductMaster(bases.BaseProductMaster):
    """Canonical product record.
    """

    id: _str
    canonicalName: _str
    upc: Optional[_str] = None
    department: Optional[_str] = None
    isActive: _bool
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    aliases: Optional[List['models.ProductAlias']] = None
    salesLineItems: Optional[List['models.SalesLineItem']] = None
    extracts: Optional[List['models.ProductExtract']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductMasterKeys']] = None,
        exclude: Optional[Iterable['types.ProductMasterKeys']] = None,
        required: Optional[Iterable['types.ProductMasterKeys']] = None,
        optional: Optional[Iterable['types.ProductMasterKeys']] = None,
        relations: Optional[Mapping['types.ProductMasterRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductMasterKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductMaster_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductMaster_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductMaster_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductMaster_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductMaster_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductMaster_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductMaster', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductMaster / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductMaster',
            }
        )
        _created_partial_types.add(name)


class ProductAlias(bases.BaseProductAlias):
    """Maps a raw/alias product name to a canonical ProductMaster.
    """

    id: _str
    rawName: _str
    productMasterId: _str
    productMaster: Optional['models.ProductMaster'] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductAliasKeys']] = None,
        exclude: Optional[Iterable['types.ProductAliasKeys']] = None,
        required: Optional[Iterable['types.ProductAliasKeys']] = None,
        optional: Optional[Iterable['types.ProductAliasKeys']] = None,
        relations: Optional[Mapping['types.ProductAliasRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductAliasKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductAlias_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductAlias_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductAlias_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductAlias_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductAlias_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductAlias_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductAlias', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductAlias / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductAlias',
            }
        )
        _created_partial_types.add(name)


class UnknownProduct(bases.BaseUnknownProduct):
    """Products that could not be mapped â€” queued for review.
    """

    id: _str
    rawName: _str
    storeCode: Optional[_str] = None
    businessDate: Optional[_str] = None
    occurrences: _int
    suggestedMatch: Optional[_str] = None
    resolvedAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UnknownProductKeys']] = None,
        exclude: Optional[Iterable['types.UnknownProductKeys']] = None,
        required: Optional[Iterable['types.UnknownProductKeys']] = None,
        optional: Optional[Iterable['types.UnknownProductKeys']] = None,
        relations: Optional[Mapping['types.UnknownProductRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UnknownProductKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UnknownProduct_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UnknownProduct_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UnknownProduct_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UnknownProduct_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "UnknownProduct" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UnknownProduct / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UnknownProduct',
            }
        )
        _created_partial_types.add(name)


class POSDataRecord(bases.BasePOSDataRecord):
    """Stores every raw POS XML upload alongside its parsed JSON representation.
    """

    id: _str
    source: _str
    raw_xml: _str
    parsed_data: 'fields.Json'
    message_type: Optional[_str] = None
    created_at: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.POSDataRecordKeys']] = None,
        exclude: Optional[Iterable['types.POSDataRecordKeys']] = None,
        required: Optional[Iterable['types.POSDataRecordKeys']] = None,
        optional: Optional[Iterable['types.POSDataRecordKeys']] = None,
        relations: Optional[Mapping['types.POSDataRecordRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.POSDataRecordKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _POSDataRecord_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _POSDataRecord_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _POSDataRecord_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _POSDataRecord_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "POSDataRecord" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid POSDataRecord / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'POSDataRecord',
            }
        )
        _created_partial_types.add(name)


class Item(bases.BaseItem):
    """Represents a product item in the catalog.
    """

    id: _str
    item_code: _str
    description: _str
    price: _float
    department_id: Optional[_str] = None
    updated_at: datetime.datetime
    created_at: datetime.datetime
    inventory: Optional['models.Inventory'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ItemKeys']] = None,
        exclude: Optional[Iterable['types.ItemKeys']] = None,
        required: Optional[Iterable['types.ItemKeys']] = None,
        optional: Optional[Iterable['types.ItemKeys']] = None,
        relations: Optional[Mapping['types.ItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Item_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Item_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Item_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Item_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Item_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Item_relational_fields:
                        raise errors.UnknownRelationalFieldError('Item', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Item / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Item',
            }
        )
        _created_partial_types.add(name)


class Inventory(bases.BaseInventory):
    """Tracks current stock levels for items.
    """

    id: _str
    item_code: _str
    item: Optional['models.Item'] = None
    quantity_on_hand: _int
    updated_at: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.InventoryKeys']] = None,
        exclude: Optional[Iterable['types.InventoryKeys']] = None,
        required: Optional[Iterable['types.InventoryKeys']] = None,
        optional: Optional[Iterable['types.InventoryKeys']] = None,
        relations: Optional[Mapping['types.InventoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.InventoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Inventory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Inventory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Inventory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Inventory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Inventory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Inventory_relational_fields:
                        raise errors.UnknownRelationalFieldError('Inventory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Inventory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Inventory',
            }
        )
        _created_partial_types.add(name)


class Sale(bases.BaseSale):
    """Represents a completed sale transaction.
    """

    id: _str
    transaction_id: _str
    timestamp: datetime.datetime
    total: _float
    tax: _float
    grand_total: _float
    payment_type: _str
    items: Optional[List['models.SaleItem']] = None
    created_at: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SaleKeys']] = None,
        exclude: Optional[Iterable['types.SaleKeys']] = None,
        required: Optional[Iterable['types.SaleKeys']] = None,
        optional: Optional[Iterable['types.SaleKeys']] = None,
        relations: Optional[Mapping['types.SaleRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SaleKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Sale_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Sale_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Sale_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Sale_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Sale_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Sale_relational_fields:
                        raise errors.UnknownRelationalFieldError('Sale', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Sale / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Sale',
            }
        )
        _created_partial_types.add(name)


class SaleItem(bases.BaseSaleItem):
    """Represents a single line item within a sale.
    """

    id: _str
    sale_id: _str
    sale: Optional['models.Sale'] = None
    sku: _str
    name: _str
    quantity: _int
    price: _float

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SaleItemKeys']] = None,
        exclude: Optional[Iterable['types.SaleItemKeys']] = None,
        required: Optional[Iterable['types.SaleItemKeys']] = None,
        optional: Optional[Iterable['types.SaleItemKeys']] = None,
        relations: Optional[Mapping['types.SaleItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SaleItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SaleItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SaleItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SaleItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SaleItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _SaleItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _SaleItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('SaleItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SaleItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SaleItem',
            }
        )
        _created_partial_types.add(name)



_User_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_User_fields: Dict['types.UserKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('username', {
            'name': 'username',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('passwordHash', {
            'name': 'passwordHash',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storeId', {
            'name': 'storeId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ImportRecord_relational_fields: Set[str] = {
        'events',
        'errors',
        'dailySalesSummaries',
        'salesLineItems',
        'departmentSummaries',
        'fuelRecords',
        'productExtracts',
    }
_ImportRecord_fields: Dict['types.ImportRecordKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('originalFileName', {
            'name': 'originalFileName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Original file name as uploaded.''',
        }),
        ('storedFilePath', {
            'name': 'storedFilePath',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Path where the raw XML is stored on disk.''',
        }),
        ('checksum', {
            'name': 'checksum',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SHA-256 checksum of the raw file bytes.''',
        }),
        ('fileSize', {
            'name': 'fileSize',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''File size in bytes.''',
        }),
        ('uploadedAt', {
            'name': 'uploadedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': '''When the file was uploaded.''',
        }),
        ('uploadedBy', {
            'name': 'uploadedBy',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Who uploaded it (user/system identifier).''',
        }),
        ('sourceType', {
            'name': 'sourceType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Source type: POSExport, NAXML-ItemMaintenanceRequest, etc.''',
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Normalized store code extracted from the file.''',
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Business date the data covers (YYYY-MM-DD).''',
        }),
        ('parserVersion', {
            'name': 'parserVersion',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Parser version used.''',
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.ImportStatus',
            'is_relational': False,
            'documentation': '''Current processing status.''',
        }),
        ('recordsExtracted', {
            'name': 'recordsExtracted',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Number of records successfully extracted.''',
        }),
        ('errorCount', {
            'name': 'errorCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Number of validation/processing errors.''',
        }),
        ('rawXml', {
            'name': 'rawXml',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''The full raw XML text preserved for reprocessing.''',
        }),
        ('events', {
            'name': 'events',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ImportEvent\']',
            'is_relational': True,
            'documentation': '''Timeline of status transitions.''',
        }),
        ('errors', {
            'name': 'errors',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ImportError\']',
            'is_relational': True,
            'documentation': '''All errors/warnings recorded during processing.''',
        }),
        ('dailySalesSummaries', {
            'name': 'dailySalesSummaries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DailySalesSummary\']',
            'is_relational': True,
            'documentation': '''Extracted normalized data''',
        }),
        ('salesLineItems', {
            'name': 'salesLineItems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesLineItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('departmentSummaries', {
            'name': 'departmentSummaries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DepartmentSummary\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('fuelRecords', {
            'name': 'fuelRecords',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FuelRecord\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('productExtracts', {
            'name': 'productExtracts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductExtract\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ImportEvent_relational_fields: Set[str] = {
        'importRecord',
    }
_ImportEvent_fields: Dict['types.ImportEventKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.ImportStatus',
            'is_relational': False,
            'documentation': '''Status at time of event.''',
        }),
        ('note', {
            'name': 'note',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Optional human-readable note.''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ImportError_relational_fields: Set[str] = {
        'importRecord',
    }
_ImportError_fields: Dict['types.ImportErrorKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('errorType', {
            'name': 'errorType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Error category: VALIDATION, PROCESSING, DUPLICATE, etc.''',
        }),
        ('message', {
            'name': 'message',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Human-readable error message.''',
        }),
        ('field', {
            'name': 'field',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Optional field/path that caused the error.''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_DailySalesSummary_relational_fields: Set[str] = {
        'importRecord',
    }
_DailySalesSummary_fields: Dict['types.DailySalesSummaryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('grossSales', {
            'name': 'grossSales',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('netSales', {
            'name': 'netSales',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionCount', {
            'name': 'transactionCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('averageTicket', {
            'name': 'averageTicket',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('cashAmount', {
            'name': 'cashAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('creditAmount', {
            'name': 'creditAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('debitAmount', {
            'name': 'debitAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('ebtAmount', {
            'name': 'ebtAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('fuelSales', {
            'name': 'fuelSales',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('discountAmount', {
            'name': 'discountAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_SalesLineItem_relational_fields: Set[str] = {
        'importRecord',
        'productMaster',
    }
_SalesLineItem_fields: Dict['types.SalesLineItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionId', {
            'name': 'transactionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('itemName', {
            'name': 'itemName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('itemCode', {
            'name': 'itemCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('upc', {
            'name': 'upc',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('department', {
            'name': 'department',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitPrice', {
            'name': 'unitPrice',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesAmount', {
            'name': 'salesAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMasterId', {
            'name': 'productMasterId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMaster', {
            'name': 'productMaster',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductMaster',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_DepartmentSummary_relational_fields: Set[str] = {
        'importRecord',
    }
_DepartmentSummary_fields: Dict['types.DepartmentSummaryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('department', {
            'name': 'department',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantitySold', {
            'name': 'quantitySold',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('grossAmount', {
            'name': 'grossAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('netAmount', {
            'name': 'netAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxAmount', {
            'name': 'taxAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('discountAmount', {
            'name': 'discountAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_FuelRecord_relational_fields: Set[str] = {
        'importRecord',
    }
_FuelRecord_fields: Dict['types.FuelRecordKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fuelGrade', {
            'name': 'fuelGrade',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gallons', {
            'name': 'gallons',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('salesAmount', {
            'name': 'salesAmount',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('pricePerGallon', {
            'name': 'pricePerGallon',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('pumpNumber', {
            'name': 'pumpNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ProductExtract_relational_fields: Set[str] = {
        'importRecord',
        'productMaster',
    }
_ProductExtract_fields: Dict['types.ProductExtractKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importId', {
            'name': 'importId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('importRecord', {
            'name': 'importRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.ImportRecord',
            'is_relational': True,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawName', {
            'name': 'rawName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('itemCode', {
            'name': 'itemCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('upc', {
            'name': 'upc',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('department', {
            'name': 'department',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitPrice', {
            'name': 'unitPrice',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMasterId', {
            'name': 'productMasterId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMaster', {
            'name': 'productMaster',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductMaster',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ProductMaster_relational_fields: Set[str] = {
        'aliases',
        'salesLineItems',
        'extracts',
    }
_ProductMaster_fields: Dict['types.ProductMasterKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('canonicalName', {
            'name': 'canonicalName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('upc', {
            'name': 'upc',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('department', {
            'name': 'department',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('aliases', {
            'name': 'aliases',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductAlias\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('salesLineItems', {
            'name': 'salesLineItems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SalesLineItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('extracts', {
            'name': 'extracts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductExtract\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductAlias_relational_fields: Set[str] = {
        'productMaster',
    }
_ProductAlias_fields: Dict['types.ProductAliasKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawName', {
            'name': 'rawName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMasterId', {
            'name': 'productMasterId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productMaster', {
            'name': 'productMaster',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductMaster',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_UnknownProduct_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_UnknownProduct_fields: Dict['types.UnknownProductKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawName', {
            'name': 'rawName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storeCode', {
            'name': 'storeCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('businessDate', {
            'name': 'businessDate',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('occurrences', {
            'name': 'occurrences',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('suggestedMatch', {
            'name': 'suggestedMatch',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolvedAt', {
            'name': 'resolvedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_POSDataRecord_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_POSDataRecord_fields: Dict['types.POSDataRecordKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('source', {
            'name': 'source',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('raw_xml', {
            'name': 'raw_xml',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('parsed_data', {
            'name': 'parsed_data',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('message_type', {
            'name': 'message_type',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Item_relational_fields: Set[str] = {
        'inventory',
    }
_Item_fields: Dict['types.ItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('item_code', {
            'name': 'item_code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('price', {
            'name': 'price',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('department_id', {
            'name': 'department_id',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('inventory', {
            'name': 'inventory',
            'is_list': False,
            'optional': True,
            'type': 'models.Inventory',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Inventory_relational_fields: Set[str] = {
        'item',
    }
_Inventory_fields: Dict['types.InventoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('item_code', {
            'name': 'item_code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('item', {
            'name': 'item',
            'is_list': False,
            'optional': True,
            'type': 'models.Item',
            'is_relational': True,
            'documentation': None,
        }),
        ('quantity_on_hand', {
            'name': 'quantity_on_hand',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Sale_relational_fields: Set[str] = {
        'items',
    }
_Sale_fields: Dict['types.SaleKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('transaction_id', {
            'name': 'transaction_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('timestamp', {
            'name': 'timestamp',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('total', {
            'name': 'total',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('tax', {
            'name': 'tax',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('grand_total', {
            'name': 'grand_total',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('payment_type', {
            'name': 'payment_type',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('items', {
            'name': 'items',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.SaleItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_SaleItem_relational_fields: Set[str] = {
        'sale',
    }
_SaleItem_fields: Dict['types.SaleItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sale_id', {
            'name': 'sale_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sale', {
            'name': 'sale',
            'is_list': False,
            'optional': True,
            'type': 'models.Sale',
            'is_relational': True,
            'documentation': None,
        }),
        ('sku', {
            'name': 'sku',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('price', {
            'name': 'price',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(User)
model_rebuild(ImportRecord)
model_rebuild(ImportEvent)
model_rebuild(ImportError)
model_rebuild(DailySalesSummary)
model_rebuild(SalesLineItem)
model_rebuild(DepartmentSummary)
model_rebuild(FuelRecord)
model_rebuild(ProductExtract)
model_rebuild(ProductMaster)
model_rebuild(ProductAlias)
model_rebuild(UnknownProduct)
model_rebuild(POSDataRecord)
model_rebuild(Item)
model_rebuild(Inventory)
model_rebuild(Sale)
model_rebuild(SaleItem)
